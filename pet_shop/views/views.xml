<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="pet_shop.list">
      <field name="name">pet_shop list</field>
      <field name="model">pet_shop.pet_shop</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="pet_shop.action_window">
      <field name="name">pet_shop window</field>
      <field name="res_model">pet_shop.pet_shop</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="pet_shop.action_server">
      <field name="name">pet_shop server</field>
      <field name="model_id" ref="model_pet_shop_pet_shop"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="pet_shop" id="pet_shop.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="pet_shop.menu_1" parent="pet_shop.menu_root"/>
    <menuitem name="Menu 2" id="pet_shop.menu_2" parent="pet_shop.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="pet_shop.menu_1_list" parent="pet_shop.menu_1"
              action="pet_shop.action_window"/>
    <menuitem name="Server to list" id="pet_shop" parent="pet_shop.menu_2"
              action="pet_shop.action_server"/>
    -->
  </data>
</odoo>